
- npm install vite@latest
- node -v, npm -v ----> to check
- npm install -g typescript
- npm create vite@latest-talentspark  ---> down arrow -->react --> typescript ---> eslint --> install.
- cd talentspark/
- npm run dev ----> run frontend
- npm install axios ---> for concurrent operations



- javascript -> ES6 -> Modern Javascript with shortfoem and less code --> arrow functions, rest and spread, template literals, destructing, promises, async/await.
- DOM - document Object Manipulation.
- Virtual DOM - react virtual DOM --> copy of original DOM which wi;; update DOM and then updated DOM will be updated in real DOM.
- Components - which are different sections of web page ----> for reusable purpose in any page by importing.
- useeffect - which is used to call the api or which is used to fetch the data from api automaticallly when the page is loaded.
- useState - which is used to store the data in the component and which will update the component when the data is updated or changed.
- axios - used to call the api from backend.
- promise - handles the asynchronous operation.
- async/await - used to handle the asynchronous operations in a synchronous way.


- In talent spark ---public : we have images for the page
- src--> assests --> stores any images for the application
- src ---> types --> Validation in frontend
- src ---> Services -->


## Whole application exists in app.tsx - all components imported 
## The application runs at main.tsx - app.tsx is imported

# FLOW OF AN APPLICATION
# React/TS(UI) ---> axios ----> localhost:8000(api call) ----> fastapi(python/Backend) ----> db ----> useEffect ----> setstate ----> re-render ----> UI 


http://localhost:5173/

